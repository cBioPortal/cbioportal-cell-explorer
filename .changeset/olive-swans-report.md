---
"@cbioportal-cell-explorer/api-client": patch
---

Regenerate types against the current API spec, which now carries harvested store
metadata (`n_obs`, `n_vars`, embedding keys) on the dataset responses.
